# Data directory

Source-like and row-level data are intentionally excluded from the public Git
repository. The acquisition and preparation scripts recreate `external/` and
`derived/` locally from authorized inputs. See the main README for the
`PM25_OBSERVATION_GLOB` input setting.
