# Model directory

Serialized research and deployment model bundles are generated by the pipeline
and excluded from Git. Their local checksums, configurations, and evaluated
results are recorded in the versioned provenance manifests and summary tables.
